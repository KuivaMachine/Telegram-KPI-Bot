tls:
  stores:
    default:
      defaultCertificate:
        certFile: /etc/traefik/certs/fullchain1.pem
        keyFile: /etc/traefik/certs/privkey1.pem
  certificates:
    - certFile: /etc/traefik/archive/fullchain1.pem
      keyFile: /etc/traefik/archive/privkey1.pem
